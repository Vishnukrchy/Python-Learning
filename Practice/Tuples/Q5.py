#Convert a Tuple to a List: Write a Python function that converts a tuple into a list.

tuple = (1, 2, 3, 4, 5)
print(list(tuple))

#Concatenate Tuples: Given two tuples (1, 2, 3) and (4, 5, 6), concatenate them into a single tuple.

tuple1 = (1, 2, 3)
tuple2 = (4, 5, 6)
concatenated_tuple = tuple1 + tuple2
print(concatenated_tuple)